/**
 * هدف فایل: re-export قرارداد خام Mapping برای سازگاری importهای Feature.
 * جایگاه معماری: features/mappings و مرز عمومی Schemaهای Mapping.
 */
export { mappingCodeSchema, mappingSchema, mappingSourceFieldSchema } from '@/shared/schemas/mapping.schema';
